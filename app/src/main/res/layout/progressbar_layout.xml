<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/progressbar_layout"
    android:layout_width="0dp"
    android:layout_height="@dimen/progress_layout_height"
    android:background="@color/progressbar_layout_bg"
    android:orientation="vertical"
    android:visibility="gone"
    app:layout_constraintBottom_toTopOf="@+id/bottom_button_layout"
    app:layout_constraintEnd_toEndOf="parent"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintTop_toBottomOf="@+id/rvFiles">

    <Button
        android:id="@+id/cancel_progress"
        android:layout_width="@dimen/progress_stop_button_width"
        android:layout_height="match_parent"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_gravity="end"
        android:layout_marginTop="0dp"
        android:layout_marginEnd="0dp"
        android:layout_marginBottom="0dp"
        android:backgroundTint="@color/button"
        android:text="@string/cancel"
        android:textColor="@color/white" />

    <ir.nardana.linearprogressbar.LinearProgressBar
        android:id="@+id/LinearProgressBar"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_alignParentBottom="true"
        android:layout_gravity="center_horizontal"
        android:layout_marginStart="@dimen/progressbar_margin_start_end"
        android:layout_marginTop="@dimen/progressbar_margin_top_bottom"
        android:layout_marginEnd="@dimen/progressbar_margin_start_end"
        android:layout_marginBottom="@dimen/progressbar_margin_top_bottom"
        android:layout_toStartOf="@+id/cancel_progress"
        app:ProgressBackground="@color/background"
        app:ProgressToBackground="@color/progressbar"
        app:RadiusAll="20"
        app:TitleProgress="@string/placeholder"
        app:TitleSize="@dimen/progressbar_title_size" />

</RelativeLayout>