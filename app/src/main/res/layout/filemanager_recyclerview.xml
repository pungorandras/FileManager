<?xml version="1.0" encoding="utf-8"?>
<androidx.recyclerview.widget.RecyclerView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/rvFiles"
    android:layout_width="0dp"
    android:layout_height="0dp"
    android:scrollbarSize="@dimen/scrollbar_size"
    android:scrollbarThumbVertical="@color/scrollbar"
    android:scrollbars="vertical"
    app:layout_constraintBottom_toTopOf="@+id/progressbar_layout"
    app:layout_constraintEnd_toEndOf="parent"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintTop_toBottomOf="@+id/top_buttons_layout"></androidx.recyclerview.widget.RecyclerView>